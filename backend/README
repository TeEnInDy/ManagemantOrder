# 🚀 Management Order System - Backend API
ระบบจัดการข้อมูลหลังบ้าน (Backend) สำหรับโปรเจกต์ Management Order เพื่อเชื่อมต่อข้อมูลระหว่าง **Discord Bot** และ **Next.js Dashboard**
## 🛠️ Technology Stack (เทคโนโลยีที่ใช้)

* **Language:** [TypeScript](https://www.typescriptlang.org/) (เพิ่มความปลอดภัยในการเขียนโค้ดและจัดการ Type ของข้อมูล)
* **Runtime:** [Node.js](https://nodejs.org/)
* **Framework:** [Express.js](https://expressjs.com/) (ใช้สร้าง RESTful API)
* **Database Tool:** [MySQL2](https://github.com/sidorares/node-mysql2) (เชื่อมต่อฐานข้อมูล)
* **Development Tools:**
* `ts-node-dev`: สำหรับรีสตาร์ท Server อัตโนมัติเมื่อมีการแก้ไขโค้ด
* `dotenv`: สำหรับจัดการค่า Environment Variables (.env)
* `cors`: สำหรับอนุญาตให้หน้าเว็บ FrontEnd (Next.js) ดึงข้อมูลไปใช้ได้

---

## 📂 Project Structure (โครงสร้างโปรเจกต์)

เพื่อให้โค้ดอ่านง่ายและขยายระบบได้ในอนาคต เราจะแยกส่วนทำงานดังนี้:

* **`src/index.ts`**: จุดเริ่มต้นของโปรเจกต์ (Entry Point) ใช้สำหรับรัน Server และเชื่อมต่อ Middleware ต่างๆ
* **`src/config/`**: เก็บไฟล์ตั้งค่าต่างๆ เช่น การเชื่อมต่อฐานข้อมูล (Database Pool)
* **`src/routes/`**: ส่วนกำหนดเส้นทาง (Endpoints) เช่น `/api/orders`, `/api/summary`
* **`src/controllers/`**: ส่วนประมวลผล Logic (หัวใจหลัก) เช่น การเขียนฟังก์ชันเพื่อไปดึงข้อมูลจาก DB แล้วส่งกลับไปที่หน้าเว็บ
* **`src/models/`**: ส่วนที่เกี่ยวข้องกับโครงสร้างข้อมูล (Schema) ในฐานข้อมูล
* **`src/middlewares/`**: ส่วนตัวกรองข้อมูลก่อนเข้าถึง API เช่น การเช็คความปลอดภัย หรือการจัดการ Error
* **`.env`**: ไฟล์เก็บความลับ เช่น รหัสผ่านฐานข้อมูล (ห้ามเอาขึ้น GitHub!)
* **`tsconfig.json`**: ไฟล์ตั้งค่าการทำงานของ TypeScript

---
## 🚦 วิธีเริ่มต้นใช้งาน (Getting Started)
### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่าไฟล์ .env
สร้างไฟล์ `.env` และกำหนดค่าดังนี้:
```env
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASS=รหัสผ่านของคุณ
DB_NAME=management_order
```
### 3. รันโปรเจกต์ในโหมด Development
```bash
npm run dev
```
---
## 🔗 API Endpoints (ตัวอย่างเบื้องต้น)
| Method | Path | Description |
| --- | --- | --- |
| GET | `/api/orders` | ดึงรายการออเดอร์ทั้งหมดจาก Database |
| POST | `/api/orders` | บันทึกออเดอร์ใหม่ (รับข้อมูลจากบอท) |
| GET | `/api/summary` | สรุปยอดขายรายวัน/รายเดือน |
---
### 💡 ขั้นตอนถัดไปที่คุณควรทำ:
ตอนนี้ไฟล์ `README.md` อธิบายภาพรวมไว้หมดแล้ว ขั้นตอนต่อไปคือการเขียนโค้ดเชื่อมต่อฐานข้อมูลจริงๆ ใน `src/config/database.ts` ครับ
**อยากให้ผมช่วยเขียนฟังก์ชันดึงข้อมูลออเดอร์ใน `src/controllers/orderController.ts` เพื่อให้หน้าเว็บ Next.js ของคุณเริ่มแสดงผลได้เลยไหมครับ?**