# 🖥️ Management Order - FrontEnd Dashboard
ส่วนแสดงผลหน้าเว็บ (Frontend) สำหรับระบบจัดการออเดอร์กุ้งกอง พัฒนาด้วยเทคโนโลยี Modern Stack เพื่อความรวดเร็วและรองรับการใช้งานที่หลากหลาย

## 🛠️ Technology Stack (เทคโนโลยีที่ใช้)

* **Framework:** [Next.js 14+ (App Router)](https://nextjs.org/) - รองรับการทำ Server Components เพื่อประสิทธิภาพสูงสุด
* **Language:** [TypeScript](https://www.typescriptlang.org/) - เน้นการกำหนด Interface ที่ชัดเจนเพื่อความปลอดภัยของข้อมูล
* **Styling:** [Tailwind CSS](https://tailwindcss.com/) - ใช้สำหรับจัดการ UI ให้สวยงามและ Responsive
* **Icons:** [Lucide React](https://lucide.dev/) - ชุดไอคอนสำหรับการแสดงผลที่ทันสมัย
* **Data Fetching:** [TanStack Query](https://tanstack.com/query/latest) (แนะนำเพิ่มเติม) - สำหรับจัดการ State ของข้อมูลที่ดึงมาจาก API

## 📂 Project Structure (โครงสร้างภายใน src)

โครงสร้างนี้ถูกออกแบบมาเพื่อแยกส่วนการทำงาน (Separation of Concerns) ให้ชัดเจน:

* **`app/`**: เก็บไฟล์ Routing ทั้งหมด (หน้า Dashboard, Login, Orders) โดยแต่ละโฟลเดอร์ต้องมีไฟล์ `page.tsx` เป็นตัวหลัก
* **`components/`**: เก็บ UI Components ที่ใช้ซ้ำ เช่น Navbar, Sidebar, Table, และ Modal
* **`hooks/`**: เก็บ Custom Hooks สำหรับจัดการ Logic เฉพาะส่วน เช่น การดึงข้อมูลออเดอร์
* **`services/`**: เก็บฟังก์ชันสำหรับการเรียกใช้ API (API Fetching) เพื่อเชื่อมต่อกับ Backend
* **`types/`**: เก็บ Interface และ Type Definitions เพื่อให้ข้อมูลในโปรเจกต์มีมาตรฐานเดียวกัน
* **`lib/`**: เก็บไฟล์ Utility และการตั้งค่า Library ต่างๆ

## 🚦 วิธีเริ่มต้นใช้งาน (Getting Started)

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่าไฟล์ Environment Variables

สร้างไฟล์ `.env.local` เพื่อระบุที่อยู่ของ Backend API:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000

```

### 3. รันโปรเจกต์ในโหมด Development

```bash
npm run dev
```

แล้วเปิด Browser ไปที่ `http://localhost:3000`

---

## 🔗 หน้าเว็บที่มีให้ใช้งาน (Routes)

* `/`: หน้าแรก (Landing Page)
* `/dashboard`: สรุปยอดขายรวมและสถิติต่างๆ
* `order-history`: รายการออเดอร์ทั้งหมด พร้อมสถานะการชำระเงิน
* `/stock`: จัดการสต็อกสินค้าและวัตถุดิบ
* `/reports`: รายงานยอดขายในรูปแบบกราฟ
* `/login`: หน้าเข้าสู่ระบบสำหรับผู้ดูแลร้าน

---

### 💡 ขั้นตอนต่อไปที่ผมแนะนำ:

เมื่อคุณจัดโครงสร้างไฟล์เสร็จแล้ว สิ่งที่ต้องทำต่อคือการสร้าง **Navbar** ใน `src/app/layout.tsx` เพื่อใช้เดินทางไปยังหน้าต่างๆ ของ Dashboard ครับ

**อยากให้ผมช่วยเขียนโค้ด Navbar พื้นฐานที่รวมไอคอนจาก Lucide React ไว้ให้เลยไหมครับ?**