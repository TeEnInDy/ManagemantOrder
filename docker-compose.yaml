version: '3.8'

services:
  # 🗄️ 1. Database (MariaDB)
  db:
    image: mariadb:10.6
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root1234  # ⚠️ ตั้งรหัสผ่าน DB ตรงนี้
      MYSQL_DATABASE: ebizuke_db
    volumes:
      - db_data:/var/lib/mysql # เก็บข้อมูลถาวร (ปิด container ข้อมูลไม่หาย)
    networks:
      - ebizuke-network

  # ⚙️ 2. Backend API
  backend:
    build: ./backend
    restart: always
    ports:
      - "9098:9098"
    environment:
      # ⚠️ สังเกตตรง host ต้องใช้ชื่อ service "db" แทน localhost
      DATABASE_URL: mysql://root:root1234@db:3306/ebizuke_db
      JWT_SECRET: supersecretkey123
      PORT: 9098
    depends_on:
      - db
    volumes:
      - ./backend/uploads:/app/uploads # ✅ Mount โฟลเดอร์ uploads ออกมาข้างนอก (รูปไม่หาย)
    networks:
      - ebizuke-network

  # 🎨 3. Frontend (Next.js)
  frontend:
    build: 
      context: ./frontend
      # 👇 (สำคัญ) ส่งค่านี้เข้าไปตอน Build ด้วย ไม่งั้น Next.js จะไม่เห็นค่านี้
      args:
        NEXT_PUBLIC_API_URL: http://dekdee2.informatics.buu.ac.th:9098/api
        # NEXT_PUBLIC_API_URL: http://localhost:9098/api
    restart: always
    ports:
      - "9099:3000"
    environment:
      # ⚠️ ตรงนี้สำคัญ! ต้องใส่ IP ของ Server มหาลัย (เพราะ Browser ยิงจากเครื่องเรา ไม่ใช่จากใน Docker)
      NEXT_PUBLIC_API_URL: http://dekdee2.informatics.buu.ac.th:9098/api
      # NEXT_PUBLIC_API_URL : http://localhost:9098/api
    depends_on:
      - backend
    networks:
      - ebizuke-network

# สร้าง Network ให้คุยกันเองได้
networks:
  ebizuke-network:
    driver: bridge

# สร้าง Volume สำหรับเก็บข้อมูล Database
volumes:
  db_data: